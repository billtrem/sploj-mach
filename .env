# === SECURITY ===
# For local dev, use DJANGO_SECRET_KEY
DJANGO_SECRET_KEY=django-insecure-7r@%=2n!8k$+1bmzjw6b@a$3xsmq&1ep%x=v47i&$o+pxz1_#l

# Set to True for local dev, False for production
DJANGO_DEBUG=True

# Uncomment DATABASE_URL in production only (Railway provides it automatically, so usually you don't set it manually)
# DATABASE_URL=postgresql://username:password@host:port/dbname

# === SUPERUSER (optional auto-create in predeploy) ===
DJANGO_SUPERUSER_USERNAME=sploj-office
DJANGO_SUPERUSER_EMAIL=admin@sploj.com
DJANGO_SUPERUSER_PASSWORD=Machynlleth25!

# === EXTRA ===
# Remove USE_SQLITE, it's no longer needed — settings.py handles switching automatically
